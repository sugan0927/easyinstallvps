{
  "name": "easyinstallvps",
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "build": "echo 'No build required'",
    "cf-deploy": "wrangler versions upload"
  },
  "devDependencies": {
    "wrangler": "^4.73.0"
  }
}
